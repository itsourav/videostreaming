
server:
  port: 8080

aws:
  services:
    bucket: "souravimageprocessing"

cloud:
  aws:
    stack.auto: false
    region.static: "us-east-1"
    credentials:
      accessKey: "**************"
      secretKey: "****************************"
